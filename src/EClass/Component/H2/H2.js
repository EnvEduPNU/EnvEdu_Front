import Input from "../Input/Input";
import * as Styled from "./Styled";

const H2 = () => {
  return (
    <Styled.Wrapper>
      <Input placeholder={"소제목을 지정해 주세요"} />
    </Styled.Wrapper>
  );
};

export default H2;
