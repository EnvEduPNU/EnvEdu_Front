import styled from "styled-components";

export const Input = styled.input`
  border: none;
  &:focus {
    outline: none;
  } /* outline 테두리 없애기 */
  font-size: inherit;
  background-color: inherit;
  color: inherit;
`;
